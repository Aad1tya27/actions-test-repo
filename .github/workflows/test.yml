name: Debug Workflow

on:
  pull_request:
    branches:
      - '**'

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              run: echo "Test workflow triggered!"